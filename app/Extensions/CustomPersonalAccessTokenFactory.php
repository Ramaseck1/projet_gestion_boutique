<?php 
/* namespace App\Extensions;

use Laravel\Passport\PersonalAccessToken as PassportPersonalAccessToken;

class CustomPersonalAccessTokenFactory extends PassportPersonalAccessToken
{
    /**
     * Ajoute des informations supplémentaires dans le token.
     *
     * @param \App\Models\User $user
     * @return array
     */
   /*  protected function addCustomClaims($user)
    {
        return array_merge(parent::getCustomClaims(), [
            'user' => [
                'id' => $user->id,
                'nom' => $user->nom,
                'prenom' => $user->prenom,
                'role_id' => $user->role_id,
            ],
        ]);
    } */
/* }  */
